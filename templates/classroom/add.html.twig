{% form_theme classroomForm 'bootstrap_5_layout.html.twig' %}
{% extends "base.html.twig" %}

{% block body %}
<div class="container col-md-8 text-center mt-3">
   <div class="card p-3 bg-light">
      <h3 class="alert alert-primary">Add New Class</h3>
      {{ form_start(classroomForm) }}
         
      {{ form_widget(classroomForm)}}
      <input class="btn btn-outline-danger" type="submit" value="Add">

      {{ form_end(classroomForm) }}
   </div>
</div>
{% endblock %}